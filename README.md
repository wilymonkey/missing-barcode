# Missing Barcode

*A simple GUI for items that don't have barcodes.*

# BUILD

nuitka --onefile --enable-plugin=tk-inter --include-package-data=TKinterModernThemes --output-filename=missing-barcode main.py
